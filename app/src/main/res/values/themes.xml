<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TaxiSharing" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="backgroundColor">#031F3E</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.TaxiSharing.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="backgroundColor">#fff</item>
    </style>

    <style name="Theme.TaxiSharing.Welcome" parent="Theme.SplashScreen">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="backgroundColor">#902020</item>
        <item name="postSplashScreenTheme">@style/Theme.Taxi.Sharing</item>
        <item name="windowSplashScreenBackground">#fff</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/launcher_icon</item>
    </style>

    <style name="Theme.TaxiSharing.ShowAvailableCars" parent="ThemeOverlay.AppCompat.Dark.ActionBar"></style>

    <style name="Theme.Taxi.Sharing" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.TaxiSharing.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.TaxiSharing.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- Base application theme. -->
    <style name="Base.Theme.TaxiSharing" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TaxiSharing.TitleStyle" parent="Theme.AppCompat">
        <item name="android:padding">9sp</item>
        <item name="background">@color/white</item>
    </style>

    <style name="Theme.TaxiSharing.TitleText" parent="Theme.AppCompat">
        <item name="android:textSize">@dimen/title_size</item>
        <item name="fontFamily">sans-serif-medium</item>
        <item name="android:textColor">#010F02</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.TaxiSharing.Welcome" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.TaxiSharing.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>